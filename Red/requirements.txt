# Dependencias directas del proyecto (probadas en Python 3.12.10, Windows 10, CPU).
# El barrido corre en CPU: la rueda de torch solo-CPU basta y es mucho mas liviana.
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Las versiones son pisos, no anclajes; entre parentesis va la version probada.

torch>=2.6            # 2.6.0
numpy>=2.0            # 2.5.1
scipy>=1.14           # 1.18.0   (scipy.io.loadmat para la base .mat)
pandas>=2.2           # 3.0.3
pyarrow>=17           # 25.0.0   (shards en parquet)
matplotlib>=3.9       # 3.11.1   (barrido/figuras.py)
cvxpy>=1.5            # 1.9.2    (solucionador convexo de referencia)
scs>=3.2              # 3.2.11   (backend de cvxpy)
psutil>=6.0           # 7.2.2    (opcional: memoria pico por corrida)
